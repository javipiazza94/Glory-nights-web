# Avances - Jueves, 6 de Marzo de 2026

## 1. 🚀 Optimización de Rendimiento (Core Web Vitals)
- Configurado `next.config.mjs` con formatos de imagen de nueva generación (AVIF/WebP) y srcsets responsivos automáticos.
- Reemplazadas todas las etiquetas `<img>` nativas por el componente `<Image>` de Next.js en: homepage, navbar (logo), sobre-nosotros (hero, equipo) y footer.
- Añadido `priority` al hero y logo para optimizar el LCP (Largest Contentful Paint).
- Añadido `preload="none"` a todos los `<video>` para evitar descargas innecesarias al cargar la página.

## 2. 🔍 SEO Semántico Pro
- Ampliados los metadatos globales en `layout.js` con `metadataBase`, `title.template`, `keywords`, `robots`, y datos del autor.
- Implementado **OpenGraph** y **Twitter Cards** en todas las páginas (conciertos, artistas, espacios, contacto, sobre nosotros).
- Creado componente reutilizable `JsonLd.js` para inyectar structured data.
- Añadido JSON-LD schema `Organization` global y `WebSite` en la homepage para mejorar los rich snippets de Google.

## 3. 📌 Navbar Sticky con Glassmorphism
- Refactorizado el navbar de `position: relative` a `position: fixed`.
- Implementado efecto glassmorphism: fondo semitransparente con `backdrop-filter: blur(4px)` inicial.
- Al hacer scroll (>50px): fondo más opaco, blur de 20px, sombra, y logo que se compacta suavemente.
- Ajustado el `padding-top` de todas las subpáginas para compensar el navbar fijo.

## 4. ✨ Micro-interacciones con Framer Motion
- Instalada dependencia `framer-motion`.
- Creado `AnimatedSection.js`: wrapper con scroll-reveal (fade-in + slide-up) configurable en dirección, delay y duración.
- Creado `AnimatedButton.js`: botón CTA con efecto hover (escala 1.05 + brillo dorado) y efecto press.
- Creado `HeroClient.js`: hero con animaciones de entrada escalonadas (kicker → título → subtítulo → CTA).
- Envueltas todas las secciones de la homepage con `<AnimatedSection>` para scroll-reveal progresivo.

## 5. 🎨 Galería Bento Grid
- Rediseñada la galería de la homepage de un layout lineal a un **bento grid de 3 columnas** con items de diferentes tamaños (grande 2×2, alto 1×2, ancho 2×1).
- Añadidas animaciones hover con zoom y brillo en las imágenes de la galería.

## 6. 📊 Social Proof Dinámico
- Creado componente `SocialProof.js` con contadores animados (+500 asistentes, 10+ conciertos, 5+ espacios, 98% satisfacción).
- Los números se incrementan desde 0 con easing cubic al entrar en el viewport.
- Integrado entre la cartelera destacada y las reseñas en la homepage.

## 7. 📋 Formularios Inteligentes
- El formulario de contacto ahora **guarda su estado en localStorage** automáticamente y lo restaura al refrescar la página.
- Añadida **validación asíncrona del email** con feedback visual inline (borde rojo + mensaje de error).
- Indicador visual "💾 Tu formulario se guarda automáticamente" para el usuario.
- Tracking de envíos mediante el sistema de analytics local.

## 8. 📈 Capa de Analytics Local (Sin Cookies)
- Añadida tabla `analytics_events` en la base de datos para almacenar eventos.
- Creado endpoint `/api/analytics` (POST público para registrar eventos, GET autenticado para consultarlos).
- Creado hook `useTrackEvent()` reutilizable en cualquier componente (fire-and-forget, sin bloquear la UI).

## 9. 🔬 A/B Testing del Hero
- Creado hook `useABTest.js` que asigna aleatoriamente una variante (A/B) al usuario y la persiste en localStorage.
- Variante A: "Tributos a la luz de las velas" / Variante B: "Conciertos que iluminan tu alma".
- Se registran automáticamente impresiones y clics en CTA vinculados a cada variante.

## 10. 🤖 Preparación Backend para IA
- Creado endpoint placeholder `/api/ai` con la estructura completa para un futuro asistente conversacional (RAG).
- Incluye contexto de la empresa (servicios, ubicaciones, contacto) preparado para embeddings.
- Instrucciones detalladas de activación documentadas en el código.
- **Sin impacto en el frontend** — funciona como documentación técnica activa.
